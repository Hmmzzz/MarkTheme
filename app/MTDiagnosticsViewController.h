#import <UIKit/UIKit.h>

NS_ASSUME_NONNULL_BEGIN

// Shows the Runtime's ABI capability report as copyable text so a tester on an
// OS build the maintainer cannot reach can report exactly which contract the
// running system failed.
@interface MTDiagnosticsViewController : UIViewController
@end

NS_ASSUME_NONNULL_END
